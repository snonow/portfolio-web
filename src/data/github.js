// NOTE: functions/api/github.js keeps its own copy of this username — Pages
// Functions deploy as a separate bundle and must not import app source.
export const GITHUB_USERNAME = "snonow";
export const GITHUB_PROFILE_URL = `https://github.com/${GITHUB_USERNAME}`;
